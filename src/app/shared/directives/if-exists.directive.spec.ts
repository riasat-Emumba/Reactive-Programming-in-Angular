import { IfExistsDirective } from './if-exists.directive';

describe('IfExistsDirective', () => {
  it('should create an instance', () => {
    const directive = new IfExistsDirective();
    expect(directive).toBeTruthy();
  });
});
